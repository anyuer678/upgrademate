# 旧代码升级器（UpgradeMate）

> 一句话：把老项目的代码贴进去，告诉我"要升级到新版本需要改哪些"，并给出改好的 diff。

## 一、项目思路

### 1. 要解决的问题
- 老项目升级框架/语言是持久痛点：Spring Boot 2→3（javax→jakarta）、Python 2→3、AngularJS→React、MySQL 5.7→8 的过时 API
- 网上教程分散且针对单一项目；手动升级要斟酌几百处关键改动，风险高耗时数天
- 现有方案两个极端：(a) 企业级（OpenRewrite/Moderne、AWS）又重又贵，面向 mainframe 大迁移；(b) 通用 agent（Claude Code）要自己规划，没有"开箱即用的升级规则"

### 2. 调研证据（2026年8月实搜）
- 大型玩家全部押注"企业级大迁移"（mainframe/COBOL/巨额单体），普通中小项目的框架升级无人做精细
- "Deprecated API 一键迁移""CVE 修复清单"没有轻量垂直产品
- 演示传播力：贴一段旧代码→出 diff→直接粘贴运行，短视频天然素材

### 3. 用户画像
- 个人/中小团队维护老项目的开发者；升级前想让 AI 出"变更清单"评估成本
- 维护一堆小工具的 nobody（想升级依赖但怕炸）

### 4. 产品形态（MVP）
本地 CLI / Web 粘贴：
```
upg --from springboot2 --to springboot3 <目录或文件>
upg --python-upgrade 2to3 某.py
upg --list-rules springboot3   # 列出该升级的所有规则（给用户先看）
输出：
  ① 变更点清单（代码位置+原因+影响面）
  ② 自动生成的补丁（--dry-run 默认，--apply 才写）
  ③ 未覆盖点提示（"这些需要人工确认"）
```

### 5. 核心杀手锏（差异化）
1. **垂直规则库驱动**：预先内置常见升级的规则表（javax→jakarta、`改为`、timezone 变更等），LLM"解释+生成 diff"，规则库保证确定性
2. **dry-run 为默认**，绝不自动改文件
3. **先规则后 AI**：先规则匹配（快且准），AI 只补"规则没覆盖到"的
4. 输出"变更风险分级"（高/中/低），高风险条目强制人工过目

### 6. 边界与免责
- 只做"该改什么"+可执行的 diff，不保证升级完全成功（免责声明）
- 不自动执行构建/测试
- 常见规则+LLM 混合，规则库可自行扩展

## 二、技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.9+ | 快速 |
| 解析 | tree-sitter（多语言）或逐行 regex | 轻量起步：regex+行级匹配 |
| diff | difflib（标准库） | 无依赖 |
| LLM | OpenAI 兼容 API（仅补充规则覆盖不到的） | key 自备 |
| CLI | argparse + click | — |

## 三、开发规范

见 `开发规范.md`。核心硬性：
1. `--dry-run` 默认；`--apply` 才写文件；写文件前自动 git 快照（备份）
2. 每次升级输出"变更单"+风险等级+漏检自查
3. 规则库 JSON 定义（rule name/pattern/replace/risk/explain）
4. LLM 只做兜底与解释，**改动默认由规则完成**（可控）

## 四、开发规划（见 开发规划.md）

Phase 1 规则引擎+javax→jakarta 规则 → Phase 2 diff/apply/备份 → Phase 3 LLM 兜底 → Phase 4 发布（含规则库文档）。

## 五、判断标准
- 对一个 springboot2 项目，30 秒出完整变更清单与 diff；重要改动识别率 ≥90%
- `--dry-run` 不改任何文件
- 无 LLM key 时也能用（纯规则模式）