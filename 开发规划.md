# 开发规划 — 旧代码升级器

> 周期：3 天。强调"规则库先行、AI 兜底"，不会因为 LLM 质量限制而不可用。

## Phase 1（半天）：核心引擎 + 第一条规则
- [ ] `upgrader.py`：读 rules.json → 匹配 → difflib 生成 diff
- [ ] 规则集 pipe+校验器（缺字段报错）
- [ ] 内置 `springboot3` 规则集（javax→jakarta 至少 3 条、配置项 2 条）
- [ ] `tests/test_rules.py` 绿
- 验证：对含 `javax.servlet.*` 的样例 java 文件，`--dry-run` 输出 diff

## Phase 2（半天）：CLI + apply/backup/restore
- [ ] `--apply` 备份+vunkan shah 文件 + `--restore` 回滚
- [ ] SHA256 审计 + `upgrade_log.json`
- [ ] 风险分级彩色输出（高/中/低）+ 退出码
- [ ] `tests/test_upgrader.py` 绿
- 验证：destroy==restore 全等（备份恢复后文件字节级相同）

## Phase 3（半天~1 天）：多语言规则集
- [ ] `python3` 规则集（xrange→range、urllib2→urllib.request、已删 module 提示）≥6 条
- [ ] `mysql8`（字符集/授权语法）≥4 条（可选）
- [ ] 逐文件多线程（ThreadPool 4）加速
- 验证：造 python2 样例目录跑通、报错清单合理

## Phase 4（1 天）：LLM 兜底 + 发布
- [ ] `upgrader.py --llm` 对 high/未覆盖文件生成"人工处理提示"(不改文件)
- [ ] README（用途：迁移清单生成器、免责、如何扩展规则集）
- [ ] `pyproject.toml`、`pytest` 全绿、GitHub 仓库

## 验收清单
- [ ] springboot2 样例：10 条内规则匹配 ≥8 条，全部输出 diff 且不可写选项默认不写
- [ ] 无 LLM key 时完整离线可用（规则驱动）
- [ ] `--apply` → `--restore` 字节一致
- [ ] regex 大文件不卡死（timeout）

## 风险与对策
| 风险 | 对策 |
|---|---|
| 正则误替换 | 每条款 rule 配 before/after 样例测试；单行替换不跨块 |
| 升级不完整 | 明确"变更清单+风险等级+人工自查"输出，不做自动化承诺 |
| 用户误运行 apply | dry-run 默认+备份+restore |