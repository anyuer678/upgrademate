# 开发规范 — 旧代码升级器

## 1. 总体约束
- **语言**：Python 3.9+；`rules.json` 数据驱动规则 + `upgrader.py` ≤ 500 行
- **依赖**：`regex`（可选）、标准库为主；LLM 调用 `httpx` 可选
- **禁止**：无备份直接覆盖；默认不得修改文件（dry-run 为默认）

## 2. CLI 接口（硬性）
```
upm --profile springboot3 --target springboot3 [--files ...] [--dir ...]
     [--diff] [--apply] [--backup] [--list-rules <profile>] [--json]
```
- 无 `--apply` 时只输出报告与 diff（**默认 dry-run**）
- `--apply` 自动生成 `.upgrade-backup/` 备份（复制原文件）后再改
- `--list-rules` 仅列规则统计（配置需要）
- 退出码：0=完成无高风险；1=有高风险项需人工；2=参数错误；3=规则集不存在

## 3. 规则库（rules.json，硬性结构）
```
{
  "id": "s2.s3.javax.jakarta",
  "profile": "springboot2→3",
  "priority": "high",
  "match": {"file": "*.java", "regex": "import javax\\.([\\w.]+);"},
  "replace": "import jakarta.\\1;",
  "explain": "Spring Boot 3 把 javax.* 迁移至 jakarta.*",
  "example_before": "...", "example_after": "..."
}
```
- 默认内置：`springboot3`（javax→jakarta、配置项变更）、`python3`（print、xrange、urllib）、`mysql8`（分词器/默认证书警告）、`vue3`（可选）
- 规则字段必填：id/profile/match/replace/explain；缺失校验报错

## 4. 执行管线（确定性优先）
1. 收集文件 → 2. 逐规则匹配（regex 单行级）+ 命中记录 → 3. 生成 unified diff（difflib）→ 4. 输出报告 → 5. 仅当 `--apply` 才写（先备份）
- LLM 兜底（可选）：对 `high-risk 且无规则命中`的文件调用 1 次 LLM 给"建议人工处理点"，**LLM 结果不改文件**，仅提示（引用文件/行）
- `--no-llm` 纯规则模式可离线

## 5. 风险分级
- `high`：编译级破坏（import/语法/配置结构）→ 报告 top 显红
- `medium`：行为可能变化（API 改名但兼容）
- `low`：文档/注释/格式化
- 汇总：统计各风险数；存在 high → 退出码 1

## 6. 安全
- `--apply` 前计算 SHA256 记录（放入 `upgrade_log.json`），恢复命令 `--restore`（回滚到备份）
- 永不删除文件；备份目录 `0600` 尽量
- 不执行生成的代码/不运行构建工具
- 规则 `regex` 必须 timeout 限制（防 ReDoS）—— regex 预编译 + `timeout 200ms/line`

## 7. 测试
- `tests/test_rules.py`：每条内置规则配 before/after 样例断言（≥10 条）
- `tests/test_upgrader.py`：dry-run 不动文件；apply 备份存在；restore 还原完整
- `tests/test_cli.py`：参数/z码/JSON 输出
- 虚构示例项目（mini springboot 库或纯 java 文件）跑 e2e

## 8. 交付物
- CLI 可用 + `rules/*.json`（至少 springboot3+python3 两套）+ README（用法、规则写作指南、免责）