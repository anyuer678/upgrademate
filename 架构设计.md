# 架构设计 — 旧代码升级器（UpgradeMate）

> 目标：规则引擎（确定性）+ LLM 兜底（解释性）分家；diff/apply/restore 三阶段管线；所有副作用集中在 executor。

## 1. 总体分层

```
┌──────────────────────────────────────────────┐
│             main.py (CLI 装配)               │
├──────────────┬──────────────┬────────────────┤
│ rules_engine │ diff_engine  │ llm_advisor   │
│ 规则匹配(纯) │ diff 生成     │ 高风险提示     │
├──────────────┴──────┬───────┴────────────────┤
│        executor.py（唯一写盘者）             │
│      apply(备份)/restore/日志                 │
└──────────────────────────────────────────────┘
```

依赖方向：`main → rules_engine/diff_engine/llm_advisor → executor`。
**禁止**：`executor` 反向 import 规则；`llm_advisor` 不写文件；`diff_engine` 纯函数。

## 2. 接口契约

### 2.1 规则模型 `rules.py`
```python
@dataclass(frozen=True)
class Rule:
    id: str
    profile: str          # "springboot2to3" / "python2to3" / "mysql5to8"
    priority: str         # high|medium|low
    match: dict           # {"file_glob": "*.java", "regex": "..."}
    replace: str          # 或 "callback": 函数引用
    explain: str
    example_before: str
    example_after: str
    options: dict = {}    # re.I, count 等

RULES_DIR = "rules/"      # 每 profile 一个 JSON；启动时全部加载并校验('id'唯一性重复抛错)
def load_rules(profile: str | None = None) -> list[Rule]
def validate_rule(rule: dict) -> Rule      # 缺字段 → RuleSchemaError

@dataclass
class Hit:
    rule: Rule
    file_path: str
    lineno: int
    before: str
    after: str

def apply_rule_to_line(rule: Rule, line: str) -> str | None
    # 未匹配 return None; 匹配-> 替换结果；始终用 re.subn 得知 count
```

### 2.2 `diff_engine.py`（纯函数）
```python
@dataclass
class FileChange:
    path: str
    original: str
    modified: str
    hits: list[Hit]

def compute_changes(files: list[Path], rules: list[Rule],
                    *, workers: int = 4) -> list[FileChange]
    # 内部: 匹配 pipeline: (1) 收集 (2) 替换 (3) 无效行跳过
    # 结果仅内存；此处零副作用
def unified_diff(original_lines, modified_lines, path) -> str  # difflib 标准
def risk_of(c: FileChange) -> dict                                # {'high':n,'medium':n,'low':n}
```

### 2.3 `executor.py`（唯一落盘点）
```python
@dataclass
class Backup:
    dir: Path
    entries: list[tuple[Path, bytes]]      # 原路径与 sha256 基元

class WriteError(Exception)

def prepare_backup(changes: list[FileChange], workdir: Path,
                   backup_dir: Path = ".upgrade-backup") -> Backup
    # 复制原文件 + 记录 sha256；权限 0600
def apply_changes(changes: list[FileChange], backup: Backup,
                  *, mode: Literal["dry-run","apply"]) -> ApplyReport
    # dry-run: 不碰 fs（写日志 dummy）; apply: 原子(先写 tmp 再 replace)
# ApplyReport{files: [...], written: int, skipped: list[(path, reason)]}
def restore_c_from_backup(backup: Backup) -> int
    # 按 sha256 比对，仅还原与备份不一致的文件；失败计数
def upgrade_log(changes) -> str   # 生成 Json 报告(审计)
    # 慎重：log 文件新增路径也走 backup 规则保护
```

### 4. `llm_advisor.py`（可选、只读解释）
```python
@dataclass
class Advice:
    file: str
    snippet: str         # 被 LLM 认为需要人工处理的行
    reason: str          # 1-2 句（"依赖外部库链接，需人工确认"）
    confidence: str

def advise(files_with_target: list[FileChange],
           cfg: LLMConfig | None) -> list[Advice]
    # 仅对 `high` 风险但规则未覆盖的行；
    # cfg=None → 返回 []（离线模式完全可用）
    # 返回结果 **永不写盘**（由 CLI 展示）
```

### 4. CLI 契约
```
upm <profile> [--files f... | --dir d] [--dry-run(默认)] [--apply]
    [--backup-dir .upgrade-backup ][--list-rules] [--json] [--no-llm]
    [--restore [--backup-dir]]
```
| 模式 | 行为 |
|---|---|
| 默认（dry） | 输出 change 列表 + diff + 风险等级 |
| --apply | 先备份后应用；结束时写 upgrade_log.json |
| --restore | 用现有备份恢复 |
| --list-rules | 列出 profile 内所有规则 id/priority/explain |
退出码：0=无 high；1=存在 high；2=参数；3=规则集不存在；4=backup 冲突。

## 5. 数据流
```
main → 加载 rules → diff_engine.compute_changes(只读)
  → dry: 输出 diff+风险         （不碰 fs）
  → apply: executor.prepare_backup → executor.apply_changes(核心原子)
  → llm_advisor.advise(high未覆盖) → 展示人工提示
  → restore: 读 backup 对应 sha → 恢复（如需）
```

## 6. 并行开发切分
| 任务 | 模块 | 依赖 |
|---|---|---|
| A | `rules.py` 模型+校验+加载（含一条 springboot 规则） | 无 |
| B | `diff_engine.py`（读 rules 接口契约） | A 类型（stub 兼容） |
| C | `executor.py`（备份/apply/restore/原子） | 无（纯 fs） |
| D | `llm_advisor.py` | LLMConfig 契约 |
| E | `main.py` 集成 | A-D |
A/B/C 并行 → D → E。

## 7. 接口核对清单
- [ ] `apply_rule_to_file` 对 `import javax.*` → `import jakarta.*` 必命中
- [ ] `compute_changes` 无副作用（允许对 temp 目录 e2e 验证）
- [ ] `executor` 原子写：失败中间态不留半文件
- [ ] `restore` 仅回差异文件，非全部
- [ ] `--dry-run` 后目标目录 mtime 不变

## 8. 安全底线
- 永不执行生成的代码/构建命令
- `--apply` 前后均校验备份存在；缺 → 拒绝执行并报错
- 规则 regex 每行带 `timeout=200ms` 防 ReDoS